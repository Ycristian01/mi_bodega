<%= form_for @invite, :url => invites_path do |f| %>
  <label><%= @invite.account_id %></label>
  <%= f.hidden_field :account_id, :value => @invite.account_id %>
  <%= f.label :email %>
  <%= f.email_field :email %>
  <%= f.submit 'Send' %>
<% end %>