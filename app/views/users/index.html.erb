<h1>This is the index</h1>
<h2><%=current_user&.email%></h2>

<%if user_signed_in? %>
  <h2>Welcome <%= current_user.email %>
  <h1><%= current_user.current_tenant_id %></h1>
  <div class="ui list">
    <div class="item">
      <%= link_to 'Log out', destroy_user_session_path, method: :delete  %>
    </div>
    <div class="item">
      <%= link_to 'New account', new_account_path %>
    </div>
    <div class="item">
      <%= link_to 'Account index', accounts_path %>
    </div>
    <div class="item">
      <%= link_to 'New invite', new_membership_path %>
    </div>
    <div class="item">
      <%= link_to 'Box index', boxes_path %>
    </div>
  </div>
  <%else%>
    <%= link_to 'Log in', new_user_session_path %>
    <%=link_to 'Sign up', new_user_registration_path %>

<%end%>